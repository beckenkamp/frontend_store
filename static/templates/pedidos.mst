<div class="product">
    <div class="product-image pull-left">
        <img class="img-responsive" src="{{image}}" alt="{{title}}">
    </div>
    <h2>{{title}}</h2>
    <p class="lead">Código: {{code}}</p>
    <p>{{description}}</p>
</div>